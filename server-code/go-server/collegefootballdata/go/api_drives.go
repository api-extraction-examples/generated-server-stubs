/*
 * College Football Data API
 *
 * This is an API for accessing all sorts of college football data.  Please note that API keys should be supplied with \"Bearer \" prepended (e.g. \"Bearer your_key\"). API keys can be acquired from the CollegeFootballData.com website.
 *
 * API version: 4.4.12
 * Contact: admin@collegefootballdata.com
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// DrivesAPIController binds http requests to an api service and writes the service results to the http response
type DrivesAPIController struct {
	service DrivesAPIServicer
	errorHandler ErrorHandler
}

// DrivesAPIOption for how the controller is set up.
type DrivesAPIOption func(*DrivesAPIController)

// WithDrivesAPIErrorHandler inject ErrorHandler into controller
func WithDrivesAPIErrorHandler(h ErrorHandler) DrivesAPIOption {
	return func(c *DrivesAPIController) {
		c.errorHandler = h
	}
}

// NewDrivesAPIController creates a default api controller
func NewDrivesAPIController(s DrivesAPIServicer, opts ...DrivesAPIOption) Router {
	controller := &DrivesAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the DrivesAPIController
func (c *DrivesAPIController) Routes() Routes {
	return Routes{
		"GetDrives": Route{
			strings.ToUpper("Get"),
			"/drives",
			c.GetDrives,
		},
	}
}

// GetDrives - Drive data and results
func (c *DrivesAPIController) GetDrives(w http.ResponseWriter, r *http.Request) {
	query := r.URL.Query()
	var yearParam int32
	if query.Has("year") {
		param, err := parseNumericParameter[int32](
			query.Get("year"),
			WithParse[int32](parseInt32),
			WithMinimum[int32](2001),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		yearParam = param
	} else {
		c.errorHandler(w, r, &RequiredError{Field: "year"}, nil)
		return
	}
	var seasonTypeParam string
	if query.Has("seasonType") {
		param := query.Get("seasonType")

		seasonTypeParam = param
	} else {
		param := regular
		seasonTypeParam = param
	}
	var weekParam int32
	if query.Has("week") {
		param, err := parseNumericParameter[int32](
			query.Get("week"),
			WithParse[int32](parseInt32),
			WithMinimum[int32](1),
			WithMaximum[int32](16),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		weekParam = param
	} else {
	}
	var teamParam string
	if query.Has("team") {
		param := query.Get("team")

		teamParam = param
	} else {
	}
	var offenseParam string
	if query.Has("offense") {
		param := query.Get("offense")

		offenseParam = param
	} else {
	}
	var defenseParam string
	if query.Has("defense") {
		param := query.Get("defense")

		defenseParam = param
	} else {
	}
	var conferenceParam string
	if query.Has("conference") {
		param := query.Get("conference")

		conferenceParam = param
	} else {
	}
	var offenseConferenceParam string
	if query.Has("offenseConference") {
		param := query.Get("offenseConference")

		offenseConferenceParam = param
	} else {
	}
	var defenseConferenceParam string
	if query.Has("defenseConference") {
		param := query.Get("defenseConference")

		defenseConferenceParam = param
	} else {
	}
	var classificationParam string
	if query.Has("classification") {
		param := query.Get("classification")

		classificationParam = param
	} else {
	}
	result, err := c.service.GetDrives(r.Context(), yearParam, seasonTypeParam, weekParam, teamParam, offenseParam, defenseParam, conferenceParam, offenseConferenceParam, defenseConferenceParam, classificationParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
