/*
 * College Football Data API
 *
 * This is an API for accessing all sorts of college football data.  Please note that API keys should be supplied with \"Bearer \" prepended (e.g. \"Bearer your_key\"). API keys can be acquired from the CollegeFootballData.com website.
 *
 * API version: 4.4.12
 * Contact: admin@collegefootballdata.com
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// CoachesAPIController binds http requests to an api service and writes the service results to the http response
type CoachesAPIController struct {
	service CoachesAPIServicer
	errorHandler ErrorHandler
}

// CoachesAPIOption for how the controller is set up.
type CoachesAPIOption func(*CoachesAPIController)

// WithCoachesAPIErrorHandler inject ErrorHandler into controller
func WithCoachesAPIErrorHandler(h ErrorHandler) CoachesAPIOption {
	return func(c *CoachesAPIController) {
		c.errorHandler = h
	}
}

// NewCoachesAPIController creates a default api controller
func NewCoachesAPIController(s CoachesAPIServicer, opts ...CoachesAPIOption) Router {
	controller := &CoachesAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the CoachesAPIController
func (c *CoachesAPIController) Routes() Routes {
	return Routes{
		"GetCoaches": Route{
			strings.ToUpper("Get"),
			"/coaches",
			c.GetCoaches,
		},
	}
}

// GetCoaches - Coaching records and history
func (c *CoachesAPIController) GetCoaches(w http.ResponseWriter, r *http.Request) {
	query := r.URL.Query()
	var firstNameParam string
	if query.Has("firstName") {
		param := query.Get("firstName")

		firstNameParam = param
	} else {
	}
	var lastNameParam string
	if query.Has("lastName") {
		param := query.Get("lastName")

		lastNameParam = param
	} else {
	}
	var teamParam string
	if query.Has("team") {
		param := query.Get("team")

		teamParam = param
	} else {
	}
	var yearParam int32
	if query.Has("year") {
		param, err := parseNumericParameter[int32](
			query.Get("year"),
			WithParse[int32](parseInt32),
			WithMinimum[int32](1869),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		yearParam = param
	} else {
	}
	var minYearParam int32
	if query.Has("minYear") {
		param, err := parseNumericParameter[int32](
			query.Get("minYear"),
			WithParse[int32](parseInt32),
			WithMinimum[int32](1869),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		minYearParam = param
	} else {
	}
	var maxYearParam int32
	if query.Has("maxYear") {
		param, err := parseNumericParameter[int32](
			query.Get("maxYear"),
			WithParse[int32](parseInt32),
			WithMinimum[int32](1869),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		maxYearParam = param
	} else {
	}
	result, err := c.service.GetCoaches(r.Context(), firstNameParam, lastNameParam, teamParam, yearParam, minYearParam, maxYearParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
