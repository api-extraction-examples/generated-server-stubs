/*
 * Account API
 *
 * The <b>Account API</b> gives sellers the ability to configure their eBay seller accounts, including the seller's policies (eBay business policies and seller-defined custom policies), opt in and out of eBay seller programs, configure sales tax tables, and get account information.  <br/><br/>For details on the availability of the methods in this API, see <a href=\"/api-docs/sell/account/overview.html#requirements\">Account API requirements and restrictions</a>.
 *
 * API version: v1.9.0
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// PaymentsProgramAPIController binds http requests to an api service and writes the service results to the http response
type PaymentsProgramAPIController struct {
	service PaymentsProgramAPIServicer
	errorHandler ErrorHandler
}

// PaymentsProgramAPIOption for how the controller is set up.
type PaymentsProgramAPIOption func(*PaymentsProgramAPIController)

// WithPaymentsProgramAPIErrorHandler inject ErrorHandler into controller
func WithPaymentsProgramAPIErrorHandler(h ErrorHandler) PaymentsProgramAPIOption {
	return func(c *PaymentsProgramAPIController) {
		c.errorHandler = h
	}
}

// NewPaymentsProgramAPIController creates a default api controller
func NewPaymentsProgramAPIController(s PaymentsProgramAPIServicer, opts ...PaymentsProgramAPIOption) Router {
	controller := &PaymentsProgramAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the PaymentsProgramAPIController
func (c *PaymentsProgramAPIController) Routes() Routes {
	return Routes{
		"GetPaymentsProgram": Route{
			strings.ToUpper("Get"),
			"/sell/account/v1/payments_program/{marketplace_id}/{payments_program_type}",
			c.GetPaymentsProgram,
		},
	}
}

// GetPaymentsProgram - 
func (c *PaymentsProgramAPIController) GetPaymentsProgram(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	marketplaceIdParam := params["marketplace_id"]
	if marketplaceIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"marketplace_id"}, nil)
		return
	}
	paymentsProgramTypeParam := params["payments_program_type"]
	if paymentsProgramTypeParam == "" {
		c.errorHandler(w, r, &RequiredError{"payments_program_type"}, nil)
		return
	}
	result, err := c.service.GetPaymentsProgram(r.Context(), marketplaceIdParam, paymentsProgramTypeParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
