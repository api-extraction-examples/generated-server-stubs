/*
 * Asana
 *
 * This is the interface for interacting with the [Asana Platform](https://developers.asana.com). Our API reference is generated from our [OpenAPI spec] (https://raw.githubusercontent.com/Asana/developer-docs/master/defs/asana_oas.yaml).
 *
 * API version: 1.0
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// TimePeriodsAPIController binds http requests to an api service and writes the service results to the http response
type TimePeriodsAPIController struct {
	service TimePeriodsAPIServicer
	errorHandler ErrorHandler
}

// TimePeriodsAPIOption for how the controller is set up.
type TimePeriodsAPIOption func(*TimePeriodsAPIController)

// WithTimePeriodsAPIErrorHandler inject ErrorHandler into controller
func WithTimePeriodsAPIErrorHandler(h ErrorHandler) TimePeriodsAPIOption {
	return func(c *TimePeriodsAPIController) {
		c.errorHandler = h
	}
}

// NewTimePeriodsAPIController creates a default api controller
func NewTimePeriodsAPIController(s TimePeriodsAPIServicer, opts ...TimePeriodsAPIOption) Router {
	controller := &TimePeriodsAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the TimePeriodsAPIController
func (c *TimePeriodsAPIController) Routes() Routes {
	return Routes{
		"GetTimePeriod": Route{
			strings.ToUpper("Get"),
			"/api/1.0/time_periods/{time_period_gid}",
			c.GetTimePeriod,
		},
		"GetTimePeriods": Route{
			strings.ToUpper("Get"),
			"/api/1.0/time_periods",
			c.GetTimePeriods,
		},
	}
}

// GetTimePeriod - Get a time period
func (c *TimePeriodsAPIController) GetTimePeriod(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	query := r.URL.Query()
	timePeriodGidParam := params["time_period_gid"]
	if timePeriodGidParam == "" {
		c.errorHandler(w, r, &RequiredError{"time_period_gid"}, nil)
		return
	}
	var optPrettyParam bool
	if query.Has("opt_pretty") {
		param, err := parseBoolParameter(
			query.Get("opt_pretty"),
			WithParse[bool](parseBool),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		optPrettyParam = param
	} else {
	}
	var optFieldsParam []string
	if query.Has("opt_fields") {
		optFieldsParam = strings.Split(query.Get("opt_fields"), ",")
	}
	result, err := c.service.GetTimePeriod(r.Context(), timePeriodGidParam, optPrettyParam, optFieldsParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// GetTimePeriods - Get time periods
func (c *TimePeriodsAPIController) GetTimePeriods(w http.ResponseWriter, r *http.Request) {
	query := r.URL.Query()
	var workspaceParam string
	if query.Has("workspace") {
		param := query.Get("workspace")

		workspaceParam = param
	} else {
		c.errorHandler(w, r, &RequiredError{Field: "workspace"}, nil)
		return
	}
	var optPrettyParam bool
	if query.Has("opt_pretty") {
		param, err := parseBoolParameter(
			query.Get("opt_pretty"),
			WithParse[bool](parseBool),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		optPrettyParam = param
	} else {
	}
	var optFieldsParam []string
	if query.Has("opt_fields") {
		optFieldsParam = strings.Split(query.Get("opt_fields"), ",")
	}
	var limitParam int32
	if query.Has("limit") {
		param, err := parseNumericParameter[int32](
			query.Get("limit"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		limitParam = param
	} else {
	}
	var offsetParam string
	if query.Has("offset") {
		param := query.Get("offset")

		offsetParam = param
	} else {
	}
	var startOnParam string
	if query.Has("start_on") {
		param := string(query.Get("start_on"))

		startOnParam = param
	} else {
	}
	var endOnParam string
	if query.Has("end_on") {
		param := string(query.Get("end_on"))

		endOnParam = param
	} else {
	}
	result, err := c.service.GetTimePeriods(r.Context(), workspaceParam, optPrettyParam, optFieldsParam, limitParam, offsetParam, startOnParam, endOnParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
