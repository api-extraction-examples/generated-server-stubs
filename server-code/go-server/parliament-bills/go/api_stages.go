/*
 * Bills API
 *
 * API to get and search for information regarding Bills, their stages, associated amendments and publications.
 *
 * API version: v1
 * Contact: softwareengineering@parliament.uk
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// StagesAPIController binds http requests to an api service and writes the service results to the http response
type StagesAPIController struct {
	service StagesAPIServicer
	errorHandler ErrorHandler
}

// StagesAPIOption for how the controller is set up.
type StagesAPIOption func(*StagesAPIController)

// WithStagesAPIErrorHandler inject ErrorHandler into controller
func WithStagesAPIErrorHandler(h ErrorHandler) StagesAPIOption {
	return func(c *StagesAPIController) {
		c.errorHandler = h
	}
}

// NewStagesAPIController creates a default api controller
func NewStagesAPIController(s StagesAPIServicer, opts ...StagesAPIOption) Router {
	controller := &StagesAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the StagesAPIController
func (c *StagesAPIController) Routes() Routes {
	return Routes{
		"ApiV1StagesGet": Route{
			strings.ToUpper("Get"),
			"/api/v1/Stages",
			c.ApiV1StagesGet,
		},
	}
}

// ApiV1StagesGet - Returns a list of Bill stages.
func (c *StagesAPIController) ApiV1StagesGet(w http.ResponseWriter, r *http.Request) {
	query := r.URL.Query()
	var skipParam int32
	if query.Has("Skip") {
		param, err := parseNumericParameter[int32](
			query.Get("Skip"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		skipParam = param
	} else {
	}
	var takeParam int32
	if query.Has("Take") {
		param, err := parseNumericParameter[int32](
			query.Get("Take"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		takeParam = param
	} else {
	}
	result, err := c.service.ApiV1StagesGet(r.Context(), skipParam, takeParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
