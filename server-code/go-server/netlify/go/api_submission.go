/*
 * Netlify's API documentation
 *
 * Netlify is a hosting service for the programmable web. It understands your documents and provides an API to handle atomic deploys of websites, manage form submissions, inject JavaScript snippets, and much more. This is a REST-style API that uses JSON for serialization and OAuth 2 for authentication.  This document is an OpenAPI reference for the Netlify API that you can explore. For more detailed instructions for common uses, please visit the [online documentation](https://www.netlify.com/docs/api/). Visit our Community forum to join the conversation about [understanding and using Netlify’s API](https://community.netlify.com/t/common-issue-understanding-and-using-netlifys-api/160).  Additionally, we have two API clients for your convenience: - [Go Client](https://github.com/netlify/open-api#go-client) - [JS Client](https://github.com/netlify/build/tree/main/packages/js-client)
 *
 * API version: 2.16.0
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// SubmissionAPIController binds http requests to an api service and writes the service results to the http response
type SubmissionAPIController struct {
	service SubmissionAPIServicer
	errorHandler ErrorHandler
}

// SubmissionAPIOption for how the controller is set up.
type SubmissionAPIOption func(*SubmissionAPIController)

// WithSubmissionAPIErrorHandler inject ErrorHandler into controller
func WithSubmissionAPIErrorHandler(h ErrorHandler) SubmissionAPIOption {
	return func(c *SubmissionAPIController) {
		c.errorHandler = h
	}
}

// NewSubmissionAPIController creates a default api controller
func NewSubmissionAPIController(s SubmissionAPIServicer, opts ...SubmissionAPIOption) Router {
	controller := &SubmissionAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the SubmissionAPIController
func (c *SubmissionAPIController) Routes() Routes {
	return Routes{
		"DeleteSubmission": Route{
			strings.ToUpper("Delete"),
			"/api/v1/submissions/{submission_id}",
			c.DeleteSubmission,
		},
		"ListFormSubmission": Route{
			strings.ToUpper("Get"),
			"/api/v1/submissions/{submission_id}",
			c.ListFormSubmission,
		},
		"ListFormSubmissions": Route{
			strings.ToUpper("Get"),
			"/api/v1/forms/{form_id}/submissions",
			c.ListFormSubmissions,
		},
		"ListSiteSubmissions": Route{
			strings.ToUpper("Get"),
			"/api/v1/sites/{site_id}/submissions",
			c.ListSiteSubmissions,
		},
	}
}

// DeleteSubmission - 
func (c *SubmissionAPIController) DeleteSubmission(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	submissionIdParam := params["submission_id"]
	if submissionIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"submission_id"}, nil)
		return
	}
	result, err := c.service.DeleteSubmission(r.Context(), submissionIdParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ListFormSubmission - 
func (c *SubmissionAPIController) ListFormSubmission(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	query := r.URL.Query()
	submissionIdParam := params["submission_id"]
	if submissionIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"submission_id"}, nil)
		return
	}
	var queryParam string
	if query.Has("query") {
		param := query.Get("query")

		queryParam = param
	} else {
	}
	var pageParam int32
	if query.Has("page") {
		param, err := parseNumericParameter[int32](
			query.Get("page"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		pageParam = param
	} else {
	}
	var perPageParam int32
	if query.Has("per_page") {
		param, err := parseNumericParameter[int32](
			query.Get("per_page"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		perPageParam = param
	} else {
	}
	result, err := c.service.ListFormSubmission(r.Context(), submissionIdParam, queryParam, pageParam, perPageParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ListFormSubmissions - 
func (c *SubmissionAPIController) ListFormSubmissions(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	query := r.URL.Query()
	formIdParam := params["form_id"]
	if formIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"form_id"}, nil)
		return
	}
	var pageParam int32
	if query.Has("page") {
		param, err := parseNumericParameter[int32](
			query.Get("page"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		pageParam = param
	} else {
	}
	var perPageParam int32
	if query.Has("per_page") {
		param, err := parseNumericParameter[int32](
			query.Get("per_page"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		perPageParam = param
	} else {
	}
	result, err := c.service.ListFormSubmissions(r.Context(), formIdParam, pageParam, perPageParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ListSiteSubmissions - 
func (c *SubmissionAPIController) ListSiteSubmissions(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	query := r.URL.Query()
	siteIdParam := params["site_id"]
	if siteIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"site_id"}, nil)
		return
	}
	var pageParam int32
	if query.Has("page") {
		param, err := parseNumericParameter[int32](
			query.Get("page"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		pageParam = param
	} else {
	}
	var perPageParam int32
	if query.Has("per_page") {
		param, err := parseNumericParameter[int32](
			query.Get("per_page"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		perPageParam = param
	} else {
	}
	result, err := c.service.ListSiteSubmissions(r.Context(), siteIdParam, pageParam, perPageParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
