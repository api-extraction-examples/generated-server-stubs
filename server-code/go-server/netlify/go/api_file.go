/*
 * Netlify's API documentation
 *
 * Netlify is a hosting service for the programmable web. It understands your documents and provides an API to handle atomic deploys of websites, manage form submissions, inject JavaScript snippets, and much more. This is a REST-style API that uses JSON for serialization and OAuth 2 for authentication.  This document is an OpenAPI reference for the Netlify API that you can explore. For more detailed instructions for common uses, please visit the [online documentation](https://www.netlify.com/docs/api/). Visit our Community forum to join the conversation about [understanding and using Netlify’s API](https://community.netlify.com/t/common-issue-understanding-and-using-netlifys-api/160).  Additionally, we have two API clients for your convenience: - [Go Client](https://github.com/netlify/open-api#go-client) - [JS Client](https://github.com/netlify/build/tree/main/packages/js-client)
 *
 * API version: 2.16.0
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"
	"os"

	"github.com/gorilla/mux"
)

// FileAPIController binds http requests to an api service and writes the service results to the http response
type FileAPIController struct {
	service FileAPIServicer
	errorHandler ErrorHandler
}

// FileAPIOption for how the controller is set up.
type FileAPIOption func(*FileAPIController)

// WithFileAPIErrorHandler inject ErrorHandler into controller
func WithFileAPIErrorHandler(h ErrorHandler) FileAPIOption {
	return func(c *FileAPIController) {
		c.errorHandler = h
	}
}

// NewFileAPIController creates a default api controller
func NewFileAPIController(s FileAPIServicer, opts ...FileAPIOption) Router {
	controller := &FileAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the FileAPIController
func (c *FileAPIController) Routes() Routes {
	return Routes{
		"GetSiteFileByPathName": Route{
			strings.ToUpper("Get"),
			"/api/v1/sites/{site_id}/files/{file_path}",
			c.GetSiteFileByPathName,
		},
		"ListSiteFiles": Route{
			strings.ToUpper("Get"),
			"/api/v1/sites/{site_id}/files",
			c.ListSiteFiles,
		},
		"UploadDeployFile": Route{
			strings.ToUpper("Put"),
			"/api/v1/deploys/{deploy_id}/files/{path}",
			c.UploadDeployFile,
		},
	}
}

// GetSiteFileByPathName - 
func (c *FileAPIController) GetSiteFileByPathName(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	siteIdParam := params["site_id"]
	if siteIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"site_id"}, nil)
		return
	}
	filePathParam := params["file_path"]
	if filePathParam == "" {
		c.errorHandler(w, r, &RequiredError{"file_path"}, nil)
		return
	}
	result, err := c.service.GetSiteFileByPathName(r.Context(), siteIdParam, filePathParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ListSiteFiles - 
func (c *FileAPIController) ListSiteFiles(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	siteIdParam := params["site_id"]
	if siteIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"site_id"}, nil)
		return
	}
	result, err := c.service.ListSiteFiles(r.Context(), siteIdParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// UploadDeployFile - 
func (c *FileAPIController) UploadDeployFile(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	query := r.URL.Query()
	deployIdParam := params["deploy_id"]
	if deployIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"deploy_id"}, nil)
		return
	}
	pathParam := params["path"]
	if pathParam == "" {
		c.errorHandler(w, r, &RequiredError{"path"}, nil)
		return
	}
	fileBodyParam := *os.File{}
	d := json.NewDecoder(r.Body)
	d.DisallowUnknownFields()
	if err := d.Decode(&fileBodyParam); err != nil {
		c.errorHandler(w, r, &ParsingError{Err: err}, nil)
		return
	}
	var sizeParam int32
	if query.Has("size") {
		param, err := parseNumericParameter[int32](
			query.Get("size"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		sizeParam = param
	} else {
	}
	result, err := c.service.UploadDeployFile(r.Context(), deployIdParam, pathParam, fileBodyParam, sizeParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
