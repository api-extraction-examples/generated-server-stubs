/*
 * Netlify's API documentation
 *
 * Netlify is a hosting service for the programmable web. It understands your documents and provides an API to handle atomic deploys of websites, manage form submissions, inject JavaScript snippets, and much more. This is a REST-style API that uses JSON for serialization and OAuth 2 for authentication.  This document is an OpenAPI reference for the Netlify API that you can explore. For more detailed instructions for common uses, please visit the [online documentation](https://www.netlify.com/docs/api/). Visit our Community forum to join the conversation about [understanding and using Netlify’s API](https://community.netlify.com/t/common-issue-understanding-and-using-netlifys-api/160).  Additionally, we have two API clients for your convenience: - [Go Client](https://github.com/netlify/open-api#go-client) - [JS Client](https://github.com/netlify/build/tree/main/packages/js-client)
 *
 * API version: 2.16.0
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// BuildLogMsgAPIController binds http requests to an api service and writes the service results to the http response
type BuildLogMsgAPIController struct {
	service BuildLogMsgAPIServicer
	errorHandler ErrorHandler
}

// BuildLogMsgAPIOption for how the controller is set up.
type BuildLogMsgAPIOption func(*BuildLogMsgAPIController)

// WithBuildLogMsgAPIErrorHandler inject ErrorHandler into controller
func WithBuildLogMsgAPIErrorHandler(h ErrorHandler) BuildLogMsgAPIOption {
	return func(c *BuildLogMsgAPIController) {
		c.errorHandler = h
	}
}

// NewBuildLogMsgAPIController creates a default api controller
func NewBuildLogMsgAPIController(s BuildLogMsgAPIServicer, opts ...BuildLogMsgAPIOption) Router {
	controller := &BuildLogMsgAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the BuildLogMsgAPIController
func (c *BuildLogMsgAPIController) Routes() Routes {
	return Routes{
		"UpdateSiteBuildLog": Route{
			strings.ToUpper("Post"),
			"/api/v1/builds/{build_id}/log",
			c.UpdateSiteBuildLog,
		},
	}
}

// UpdateSiteBuildLog - 
func (c *BuildLogMsgAPIController) UpdateSiteBuildLog(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	buildIdParam := params["build_id"]
	if buildIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"build_id"}, nil)
		return
	}
	msgParam := BuildLogMsg{}
	d := json.NewDecoder(r.Body)
	d.DisallowUnknownFields()
	if err := d.Decode(&msgParam); err != nil {
		c.errorHandler(w, r, &ParsingError{Err: err}, nil)
		return
	}
	if err := AssertBuildLogMsgRequired(msgParam); err != nil {
		c.errorHandler(w, r, err, nil)
		return
	}
	if err := AssertBuildLogMsgConstraints(msgParam); err != nil {
		c.errorHandler(w, r, err, nil)
		return
	}
	result, err := c.service.UpdateSiteBuildLog(r.Context(), buildIdParam, msgParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
