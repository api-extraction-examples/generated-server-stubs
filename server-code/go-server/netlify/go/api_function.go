/*
 * Netlify's API documentation
 *
 * Netlify is a hosting service for the programmable web. It understands your documents and provides an API to handle atomic deploys of websites, manage form submissions, inject JavaScript snippets, and much more. This is a REST-style API that uses JSON for serialization and OAuth 2 for authentication.  This document is an OpenAPI reference for the Netlify API that you can explore. For more detailed instructions for common uses, please visit the [online documentation](https://www.netlify.com/docs/api/). Visit our Community forum to join the conversation about [understanding and using Netlify’s API](https://community.netlify.com/t/common-issue-understanding-and-using-netlifys-api/160).  Additionally, we have two API clients for your convenience: - [Go Client](https://github.com/netlify/open-api#go-client) - [JS Client](https://github.com/netlify/build/tree/main/packages/js-client)
 *
 * API version: 2.16.0
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"
	"os"

	"github.com/gorilla/mux"
)

// FunctionAPIController binds http requests to an api service and writes the service results to the http response
type FunctionAPIController struct {
	service FunctionAPIServicer
	errorHandler ErrorHandler
}

// FunctionAPIOption for how the controller is set up.
type FunctionAPIOption func(*FunctionAPIController)

// WithFunctionAPIErrorHandler inject ErrorHandler into controller
func WithFunctionAPIErrorHandler(h ErrorHandler) FunctionAPIOption {
	return func(c *FunctionAPIController) {
		c.errorHandler = h
	}
}

// NewFunctionAPIController creates a default api controller
func NewFunctionAPIController(s FunctionAPIServicer, opts ...FunctionAPIOption) Router {
	controller := &FunctionAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the FunctionAPIController
func (c *FunctionAPIController) Routes() Routes {
	return Routes{
		"UploadDeployFunction": Route{
			strings.ToUpper("Put"),
			"/api/v1/deploys/{deploy_id}/functions/{name}",
			c.UploadDeployFunction,
		},
	}
}

// UploadDeployFunction - 
func (c *FunctionAPIController) UploadDeployFunction(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	query := r.URL.Query()
	deployIdParam := params["deploy_id"]
	if deployIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"deploy_id"}, nil)
		return
	}
	nameParam := params["name"]
	if nameParam == "" {
		c.errorHandler(w, r, &RequiredError{"name"}, nil)
		return
	}
	fileBodyParam := *os.File{}
	d := json.NewDecoder(r.Body)
	d.DisallowUnknownFields()
	if err := d.Decode(&fileBodyParam); err != nil {
		c.errorHandler(w, r, &ParsingError{Err: err}, nil)
		return
	}
	var runtimeParam string
	if query.Has("runtime") {
		param := query.Get("runtime")

		runtimeParam = param
	} else {
	}
	var sizeParam int32
	if query.Has("size") {
		param, err := parseNumericParameter[int32](
			query.Get("size"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		sizeParam = param
	} else {
	}
	xNfRetryCountParam := r.Header.Get("X-Nf-Retry-Count")
	result, err := c.service.UploadDeployFunction(r.Context(), deployIdParam, nameParam, fileBodyParam, runtimeParam, sizeParam, xNfRetryCountParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
