/*
 * Netlify's API documentation
 *
 * Netlify is a hosting service for the programmable web. It understands your documents and provides an API to handle atomic deploys of websites, manage form submissions, inject JavaScript snippets, and much more. This is a REST-style API that uses JSON for serialization and OAuth 2 for authentication.  This document is an OpenAPI reference for the Netlify API that you can explore. For more detailed instructions for common uses, please visit the [online documentation](https://www.netlify.com/docs/api/). Visit our Community forum to join the conversation about [understanding and using Netlify’s API](https://community.netlify.com/t/common-issue-understanding-and-using-netlifys-api/160).  Additionally, we have two API clients for your convenience: - [Go Client](https://github.com/netlify/open-api#go-client) - [JS Client](https://github.com/netlify/build/tree/main/packages/js-client)
 *
 * API version: 2.16.0
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// TicketAPIController binds http requests to an api service and writes the service results to the http response
type TicketAPIController struct {
	service TicketAPIServicer
	errorHandler ErrorHandler
}

// TicketAPIOption for how the controller is set up.
type TicketAPIOption func(*TicketAPIController)

// WithTicketAPIErrorHandler inject ErrorHandler into controller
func WithTicketAPIErrorHandler(h ErrorHandler) TicketAPIOption {
	return func(c *TicketAPIController) {
		c.errorHandler = h
	}
}

// NewTicketAPIController creates a default api controller
func NewTicketAPIController(s TicketAPIServicer, opts ...TicketAPIOption) Router {
	controller := &TicketAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the TicketAPIController
func (c *TicketAPIController) Routes() Routes {
	return Routes{
		"CreateTicket": Route{
			strings.ToUpper("Post"),
			"/api/v1/oauth/tickets",
			c.CreateTicket,
		},
		"ShowTicket": Route{
			strings.ToUpper("Get"),
			"/api/v1/oauth/tickets/{ticket_id}",
			c.ShowTicket,
		},
	}
}

// CreateTicket - 
func (c *TicketAPIController) CreateTicket(w http.ResponseWriter, r *http.Request) {
	query := r.URL.Query()
	var clientIdParam string
	if query.Has("client_id") {
		param := query.Get("client_id")

		clientIdParam = param
	} else {
		c.errorHandler(w, r, &RequiredError{Field: "client_id"}, nil)
		return
	}
	result, err := c.service.CreateTicket(r.Context(), clientIdParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ShowTicket - 
func (c *TicketAPIController) ShowTicket(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	ticketIdParam := params["ticket_id"]
	if ticketIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"ticket_id"}, nil)
		return
	}
	result, err := c.service.ShowTicket(r.Context(), ticketIdParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
