/*
 * Netlify's API documentation
 *
 * Netlify is a hosting service for the programmable web. It understands your documents and provides an API to handle atomic deploys of websites, manage form submissions, inject JavaScript snippets, and much more. This is a REST-style API that uses JSON for serialization and OAuth 2 for authentication.  This document is an OpenAPI reference for the Netlify API that you can explore. For more detailed instructions for common uses, please visit the [online documentation](https://www.netlify.com/docs/api/). Visit our Community forum to join the conversation about [understanding and using Netlify’s API](https://community.netlify.com/t/common-issue-understanding-and-using-netlifys-api/160).  Additionally, we have two API clients for your convenience: - [Go Client](https://github.com/netlify/open-api#go-client) - [JS Client](https://github.com/netlify/build/tree/main/packages/js-client)
 *
 * API version: 2.16.0
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// AuditLogAPIController binds http requests to an api service and writes the service results to the http response
type AuditLogAPIController struct {
	service AuditLogAPIServicer
	errorHandler ErrorHandler
}

// AuditLogAPIOption for how the controller is set up.
type AuditLogAPIOption func(*AuditLogAPIController)

// WithAuditLogAPIErrorHandler inject ErrorHandler into controller
func WithAuditLogAPIErrorHandler(h ErrorHandler) AuditLogAPIOption {
	return func(c *AuditLogAPIController) {
		c.errorHandler = h
	}
}

// NewAuditLogAPIController creates a default api controller
func NewAuditLogAPIController(s AuditLogAPIServicer, opts ...AuditLogAPIOption) Router {
	controller := &AuditLogAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the AuditLogAPIController
func (c *AuditLogAPIController) Routes() Routes {
	return Routes{
		"ListAccountAuditEvents": Route{
			strings.ToUpper("Get"),
			"/api/v1/accounts/{account_id}/audit",
			c.ListAccountAuditEvents,
		},
	}
}

// ListAccountAuditEvents - 
func (c *AuditLogAPIController) ListAccountAuditEvents(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	query := r.URL.Query()
	accountIdParam := params["account_id"]
	if accountIdParam == "" {
		c.errorHandler(w, r, &RequiredError{"account_id"}, nil)
		return
	}
	var queryParam string
	if query.Has("query") {
		param := query.Get("query")

		queryParam = param
	} else {
	}
	var logTypeParam string
	if query.Has("log_type") {
		param := query.Get("log_type")

		logTypeParam = param
	} else {
	}
	var pageParam int32
	if query.Has("page") {
		param, err := parseNumericParameter[int32](
			query.Get("page"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		pageParam = param
	} else {
	}
	var perPageParam int32
	if query.Has("per_page") {
		param, err := parseNumericParameter[int32](
			query.Get("per_page"),
			WithParse[int32](parseInt32),
		)
		if err != nil {
			c.errorHandler(w, r, &ParsingError{Err: err}, nil)
			return
		}

		perPageParam = param
	} else {
	}
	result, err := c.service.ListAccountAuditEvents(r.Context(), accountIdParam, queryParam, logTypeParam, pageParam, perPageParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
