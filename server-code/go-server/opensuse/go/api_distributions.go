/*
 * Open Build Service API
 *
 * The _Open Build Service API_ is a XML API.  To authenticate, use [HTTP basic authentication](https://en.wikipedia.org/wiki/Basic_access_authentication) by passing the _Authorization_ header in the form of `Authorization: Basic <credentials>`.  There is no API versioning as there is no need for it right now.  Only rudimentary rate limiting is implemented, so please be gentle when using the API concurrently, especially with potentially expensive operations. In case of abuse, we will limit/remove your access.  For command-line users, we recommend using [osc](https://github.com/openSUSE/osc) with its _api_ command to interact with the API. It's as simple as this example: `osc api /about` (_about_ is one of the endpoints documented below) 
 *
 * API version: 2.10.50
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// DistributionsAPIController binds http requests to an api service and writes the service results to the http response
type DistributionsAPIController struct {
	service DistributionsAPIServicer
	errorHandler ErrorHandler
}

// DistributionsAPIOption for how the controller is set up.
type DistributionsAPIOption func(*DistributionsAPIController)

// WithDistributionsAPIErrorHandler inject ErrorHandler into controller
func WithDistributionsAPIErrorHandler(h ErrorHandler) DistributionsAPIOption {
	return func(c *DistributionsAPIController) {
		c.errorHandler = h
	}
}

// NewDistributionsAPIController creates a default api controller
func NewDistributionsAPIController(s DistributionsAPIServicer, opts ...DistributionsAPIOption) Router {
	controller := &DistributionsAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the DistributionsAPIController
func (c *DistributionsAPIController) Routes() Routes {
	return Routes{
		"DistributionsBulkReplacePut": Route{
			strings.ToUpper("Put"),
			"/distributions/bulk_replace",
			c.DistributionsBulkReplacePut,
		},
		"DistributionsDistributionIdDelete": Route{
			strings.ToUpper("Delete"),
			"/distributions/{distribution_id}",
			c.DistributionsDistributionIdDelete,
		},
		"DistributionsDistributionIdGet": Route{
			strings.ToUpper("Get"),
			"/distributions/{distribution_id}",
			c.DistributionsDistributionIdGet,
		},
		"DistributionsDistributionIdPut": Route{
			strings.ToUpper("Put"),
			"/distributions/{distribution_id}",
			c.DistributionsDistributionIdPut,
		},
		"DistributionsGet": Route{
			strings.ToUpper("Get"),
			"/distributions",
			c.DistributionsGet,
		},
		"DistributionsIncludeRemotesGet": Route{
			strings.ToUpper("Get"),
			"/distributions/include_remotes",
			c.DistributionsIncludeRemotesGet,
		},
		"DistributionsPost": Route{
			strings.ToUpper("Post"),
			"/distributions",
			c.DistributionsPost,
		},
	}
}

// DistributionsBulkReplacePut - Bulk replace all distributions.
func (c *DistributionsAPIController) DistributionsBulkReplacePut(w http.ResponseWriter, r *http.Request) {
	bodyParam := Object{}
	d := json.NewDecoder(r.Body)
	d.DisallowUnknownFields()
	if err := d.Decode(&bodyParam); err != nil && !errors.Is(err, io.EOF) {
		c.errorHandler(w, r, &ParsingError{Err: err}, nil)
		return
	}
	result, err := c.service.DistributionsBulkReplacePut(r.Context(), bodyParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// DistributionsDistributionIdDelete - Delete a distribution.
func (c *DistributionsAPIController) DistributionsDistributionIdDelete(w http.ResponseWriter, r *http.Request) {
	result, err := c.service.DistributionsDistributionIdDelete(r.Context())
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// DistributionsDistributionIdGet - Show a distribution.
func (c *DistributionsAPIController) DistributionsDistributionIdGet(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	distributionIdParam, err := parseNumericParameter[int32](
		params["distribution_id"],
		WithRequire[int32](parseInt32),
	)
	if err != nil {
		c.errorHandler(w, r, &ParsingError{Err: err}, nil)
		return
	}
	result, err := c.service.DistributionsDistributionIdGet(r.Context(), distributionIdParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// DistributionsDistributionIdPut - Update a distribution.
func (c *DistributionsAPIController) DistributionsDistributionIdPut(w http.ResponseWriter, r *http.Request) {
	bodyParam := Object{}
	d := json.NewDecoder(r.Body)
	d.DisallowUnknownFields()
	if err := d.Decode(&bodyParam); err != nil && !errors.Is(err, io.EOF) {
		c.errorHandler(w, r, &ParsingError{Err: err}, nil)
		return
	}
	result, err := c.service.DistributionsDistributionIdPut(r.Context(), bodyParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// DistributionsGet - List all distributions.
func (c *DistributionsAPIController) DistributionsGet(w http.ResponseWriter, r *http.Request) {
	result, err := c.service.DistributionsGet(r.Context())
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// DistributionsIncludeRemotesGet - List all distributions including remote.
func (c *DistributionsAPIController) DistributionsIncludeRemotesGet(w http.ResponseWriter, r *http.Request) {
	result, err := c.service.DistributionsIncludeRemotesGet(r.Context())
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// DistributionsPost - Create a distribution.
func (c *DistributionsAPIController) DistributionsPost(w http.ResponseWriter, r *http.Request) {
	distributionsPostRequestParam := DistributionsPostRequest{}
	d := json.NewDecoder(r.Body)
	d.DisallowUnknownFields()
	if err := d.Decode(&distributionsPostRequestParam); err != nil && !errors.Is(err, io.EOF) {
		c.errorHandler(w, r, &ParsingError{Err: err}, nil)
		return
	}
	if err := AssertDistributionsPostRequestRequired(distributionsPostRequestParam); err != nil {
		c.errorHandler(w, r, err, nil)
		return
	}
	if err := AssertDistributionsPostRequestConstraints(distributionsPostRequestParam); err != nil {
		c.errorHandler(w, r, err, nil)
		return
	}
	result, err := c.service.DistributionsPost(r.Context(), distributionsPostRequestParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
