/*
 * Open Build Service API
 *
 * The _Open Build Service API_ is a XML API.  To authenticate, use [HTTP basic authentication](https://en.wikipedia.org/wiki/Basic_access_authentication) by passing the _Authorization_ header in the form of `Authorization: Basic <credentials>`.  There is no API versioning as there is no need for it right now.  Only rudimentary rate limiting is implemented, so please be gentle when using the API concurrently, especially with potentially expensive operations. In case of abuse, we will limit/remove your access.  For command-line users, we recommend using [osc](https://github.com/openSUSE/osc) with its _api_ command to interact with the API. It's as simple as this example: `osc api /about` (_about_ is one of the endpoints documented below) 
 *
 * API version: 2.10.50
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// GeneralInformationAPIController binds http requests to an api service and writes the service results to the http response
type GeneralInformationAPIController struct {
	service GeneralInformationAPIServicer
	errorHandler ErrorHandler
}

// GeneralInformationAPIOption for how the controller is set up.
type GeneralInformationAPIOption func(*GeneralInformationAPIController)

// WithGeneralInformationAPIErrorHandler inject ErrorHandler into controller
func WithGeneralInformationAPIErrorHandler(h ErrorHandler) GeneralInformationAPIOption {
	return func(c *GeneralInformationAPIController) {
		c.errorHandler = h
	}
}

// NewGeneralInformationAPIController creates a default api controller
func NewGeneralInformationAPIController(s GeneralInformationAPIServicer, opts ...GeneralInformationAPIOption) Router {
	controller := &GeneralInformationAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the GeneralInformationAPIController
func (c *GeneralInformationAPIController) Routes() Routes {
	return Routes{
		"AboutGet": Route{
			strings.ToUpper("Get"),
			"/about",
			c.AboutGet,
		},
		"ArchitecturesArchitectureNameGet": Route{
			strings.ToUpper("Get"),
			"/architectures/{architecture_name}",
			c.ArchitecturesArchitectureNameGet,
		},
		"ArchitecturesGet": Route{
			strings.ToUpper("Get"),
			"/architectures",
			c.ArchitecturesGet,
		},
	}
}

// AboutGet - Get information about API.
func (c *GeneralInformationAPIController) AboutGet(w http.ResponseWriter, r *http.Request) {
	result, err := c.service.AboutGet(r.Context())
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ArchitecturesArchitectureNameGet - Show one architecture.
func (c *GeneralInformationAPIController) ArchitecturesArchitectureNameGet(w http.ResponseWriter, r *http.Request) {
	params := mux.Vars(r)
	architectureNameParam := params["architecture_name"]
	if architectureNameParam == "" {
		c.errorHandler(w, r, &RequiredError{"architecture_name"}, nil)
		return
	}
	result, err := c.service.ArchitecturesArchitectureNameGet(r.Context(), architectureNameParam)
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}

// ArchitecturesGet - List all known architectures.
func (c *GeneralInformationAPIController) ArchitecturesGet(w http.ResponseWriter, r *http.Request) {
	result, err := c.service.ArchitecturesGet(r.Context())
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
