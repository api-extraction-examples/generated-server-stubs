/*
 * The Jira Cloud platform REST API
 *
 * Jira Cloud platform REST API documentation
 *
 * API version: 1001.0.0-SNAPSHOT
 * Contact: ecosystem@atlassian.com
 * Generated by: OpenAPI Generator (https://openapi-generator.tech)
 */

package openapi

import (
	"encoding/json"
	"net/http"
	"strings"

	"github.com/gorilla/mux"
)

// ServerInfoAPIController binds http requests to an api service and writes the service results to the http response
type ServerInfoAPIController struct {
	service ServerInfoAPIServicer
	errorHandler ErrorHandler
}

// ServerInfoAPIOption for how the controller is set up.
type ServerInfoAPIOption func(*ServerInfoAPIController)

// WithServerInfoAPIErrorHandler inject ErrorHandler into controller
func WithServerInfoAPIErrorHandler(h ErrorHandler) ServerInfoAPIOption {
	return func(c *ServerInfoAPIController) {
		c.errorHandler = h
	}
}

// NewServerInfoAPIController creates a default api controller
func NewServerInfoAPIController(s ServerInfoAPIServicer, opts ...ServerInfoAPIOption) Router {
	controller := &ServerInfoAPIController{
		service:      s,
		errorHandler: DefaultErrorHandler,
	}

	for _, opt := range opts {
		opt(controller)
	}

	return controller
}

// Routes returns all the api routes for the ServerInfoAPIController
func (c *ServerInfoAPIController) Routes() Routes {
	return Routes{
		"GetServerInfo": Route{
			strings.ToUpper("Get"),
			"/rest/api/3/serverInfo",
			c.GetServerInfo,
		},
	}
}

// GetServerInfo - Get Jira instance info
func (c *ServerInfoAPIController) GetServerInfo(w http.ResponseWriter, r *http.Request) {
	result, err := c.service.GetServerInfo(r.Context())
	// If an error occurred, encode the error with the status code
	if err != nil {
		c.errorHandler(w, r, err, &result)
		return
	}
	// If no error, encode the body and the result code
	EncodeJSONResponse(result.Body, &result.Code, w)
}
